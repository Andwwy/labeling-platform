# OS / editor
.DS_Store
.idea
.vscode

# Node
/node_modules

# Local secrets — MotherDuck token + DB name, read by labeling-platform.html
# via fetch('.env'). Use .env.example as the shared template.
.env
.env.local
*.duckdb

# Generated by scripts/build-vercel-config.js from MOTHERDUCK_TOKEN /
# MOTHERDUCK_DATABASE Vercel env vars. Contains the token in cleartext — never
# commit. Recreated on every Vercel deploy and on demand for local dev.
config.js

# Vercel local
.vercel
